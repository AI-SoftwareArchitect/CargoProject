pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'CargoProject'
include 'models', 'repository', 'services', 'security', 'controllers', 'utils'

